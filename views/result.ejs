<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Download Result</title>

  <!-- Outfit Font and Bootstrap -->
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;600;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">

</head>
<body>

  <div class="container text-center mt-5">
    <h1 class="mb-4">Your Download Links</h1>

    <% if (stream) { %>
      <div>
        <video controls width="720" autoplay>
          <source src="<%= stream %>" type="video/mp4" />
          Your browser does not support the video tag.
        </video>
      </div>
      <a href="/download?url=<%= encodeURIComponent(stream) %>" class="btn btn-primary btn-lg m-2">Download Video</a>
    <% } %>

    <% if (audioStream) { %>
      <div>
        <audio controls>
          <source src="<%= audioStream %>" type="audio/mp3">
          Your browser does not support the audio element.
        </audio>
      </div>
      <a href="/download-mp3?url=<%= encodeURIComponent(audioStream) %>" class="btn btn-success btn-lg m-2">Download Audio</a>
    <% } %>

    <div class="mt-4">
      <a href="/" class="btn btn-outline-secondary">Back to Home</a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
